一对一查询主对象时对从对象是立即加载，所以不需要配cascade="all"
加载主对象时只有一条查询语句，默认是左外连接查询，相当于预加载

一对一查询从对象时对主对象是延迟加载，所以如果需要抓取主对象时必须配lazy="false"或Hibernate.initialize(account.getEmployee());
加载从对象时分两次查询，有两条查询语句
如果想只有一条查询语句，则可以配配置fetch="join"预加载，效率高些，左外连接
