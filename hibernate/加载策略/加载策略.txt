1.立即加载：立即加载需要在配置文件添加属性lazy="false"。当hibernate从数据库中取得字段值组装好一个对象后，会立即再组装此对象所关联的对象，如果这个对象还有关联对象，再
组装这个关联对象，对于一对一和多对一默认是采用立即加载，所以不需要配置。多对多和一对多则需要配置lazy="false"
2.延迟加载：当组装完一个对象后，不会立即组装和它关联的对象。
3.预先抓取：fetch="join"，和立即加载相比，预先抓取可以减少sql语句条数，提高查询速度。
4.批量加载：批量加载总是和立即加载和延迟加载联系在一起的，预先抓取则无法批量加载，会出现重复现象。